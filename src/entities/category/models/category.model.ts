import { createDomain } from 'effector';

const categoryDomain = createDomain();

const inputs = {
  //
}

const outputs = {
  //
}

const internals = {
  //
}

export {
  inputs,
  outputs,
  internals,
}